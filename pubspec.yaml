name: easyql
description: A simple and easy-to-use SQLite utility package for Dart and Flutter projects.
version: 3.0.0
homepage: https://github.com/Saravanan260203/EasyQL
repository: https://github.com/Saravanan260203/EasyQL

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

# Add regular dependencies here.
dependencies:
  sqflite: ^2.4.1
  path: ^1.9.0
  args: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  # Add assets, fonts, or other package-specific configurations here.
